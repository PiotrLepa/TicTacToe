import 'package:flutter/material.dart';

class GameResultsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Game Results Page"),
      ),
    );
  }
}
